
import 'package:flutter/material.dart';

class ScreenSnapper extends StatelessWidget {
  const ScreenSnapper({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}